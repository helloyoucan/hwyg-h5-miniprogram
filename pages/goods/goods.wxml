<header goodsTypes="{{goodsTypes}}" activeType="{{activeType}}" bind:onTapType="onTapGoodsType"/>
<filter/>
<view class="goodsList">
<goodsItem wx:for="{{activeListData}}" wx:key="{{index}}" goods="{{item}}"/>
</view>